# Changelog

## Unreleased

- Pages catalog: color-coded categories and plugins, Start here strip, job shortcuts, Copy PROFILE (body) plus Copy URL, first-task install steps on every card
- Colorful header art; `docs/profiles.json` for one-click PROFILE copy

## 1.1.0 — 2026-08-29

- Installer-first distribution: START.md is the hero path (no Node)
- Pages catalog copies raw PROFILE URLs; team cards copy the Setup tap name
- Machine API: `docs/catalog.json`, `docs/api/v1/*`, `docs/llms.txt`, `docs/agent.md`
- `grokbot score --badge` / `--md` (SVG + shields.io)
- Optional `share_url` on templates + `catalog/share-links.json`
- GitHub npx documented as the CLI that works today; npm publish on `v*` tags
- Launch notes, hub listing blurbs, share-link and npm publish runbooks

## 1.0.0 — 2026-08-29

- Initial public catalog: machine-validated Grok Bot operating contracts
- CLI `@cobusgreyling/grokbot` — list, show, init, teams, score, validate, doctor, start, new
- Bot Ready score (100) and L0–L3 autonomy
- Teams, skills (six-field official shape), routines (`test_first: true`), design patterns
- START.md installer for Grok Bot
- Convert guides (ChatGPT, Claude, Cursor, OpenClaw)
- GitHub Pages catalog
- CI: schema, secrets scan, render drift, tests
