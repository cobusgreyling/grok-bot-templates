# Convert a system prompt

You were given this page. Fetch it if you only have the URL.

The owner will attach a system prompt. Convert it into a Grok Bot operating contract. Draft only. Do not create Bots until they say so.

1. Extract: name, one job, sources, deliverable, never-list, first task.
2. Delete personality costumes that do not change the job.
3. If there is no never-list, add L1 defaults: never send, post, pay, merge, publish, or change production.
4. If the prompt says "you can do anything", refuse to convert until they pick one outcome.
5. Fill `template.yaml` fields from SPEC.md. Score mentally. Rewrite under 80.
6. Show the PROFILE. Ask before creating the Bot.

Return the YAML, not a lecture.
